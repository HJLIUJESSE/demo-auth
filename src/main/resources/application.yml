spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo_auth?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Taipei&characterEncoding=utf8
    username: demo_user
    password: StrongPassword123!
  jpa:
    hibernate:
      ddl-auto: update   # 開發期先用 update；正式請改成 validate 或 schema migration (Flyway)
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: "replace-with-a-long-random-secret-64+chars"
  expiration-minutes: 120
